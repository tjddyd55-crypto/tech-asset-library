# 손사인 기능 원본 위치

## 원본 프로젝트

- Repository: `tjddyd55-crypto/insurance`
- 기능명: 손사인 / 전자서명

## 현재 상태

아직 정확한 원본 파일 위치는 확정하지 않았다.

보험 프로젝트에서 손사인 기능은 개발 완료된 것으로 기록되어 있으나, GitHub 검색 인덱스에서 다음 키워드로는 바로 파일이 확인되지 않았다.

- `signature`
- `sign`
- `canvas`
- `서명`
- `손사인`

## 다음 확인 방법

Cursor 또는 로컬 검색에서 아래 방식으로 확인한다.

```bash
# Windows PowerShell 예시
cd D:\workspace\insurance
rg -n "signature|sign|canvas|서명|손사인|draw|pointer|touch" src server
```

또는 GitHub에서 파일 트리 기준으로 아래 후보를 확인한다.

- `src/features/*`
- `src/components/*`
- `src/pages/*`
- `src/application/*`
- `src/features/application/*`
- `server/apis/*`

## 위치 기록 템플릿

확인 후 아래 항목을 채운다.

### 프론트 컴포넌트

- 경로:
- 역할:

### 저장/API 연결부

- 경로:
- 역할:

### PDF 연동부

- 경로:
- 역할:

### 관련 스타일

- 경로:
- 역할:

## 주의

이 문서는 원본 위치를 기록하기 위한 문서다. 실제 코드는 아직 복사하지 않는다.
